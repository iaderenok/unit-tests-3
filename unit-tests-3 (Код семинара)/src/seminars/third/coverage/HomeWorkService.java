package seminars.third.coverage;

public class HomeWorkService {
    public String numberInInterval(int i) {
        if (i > 25 && i < 100) return "in interval";
        // вместо этого напишите свою реализацию
        return null;
    }

    public String numberParity(int i) {
        if (i % 2 == 0) return "Parity";
        if (i % 2 == 1) return "not Parity";
        // вместо этого напишите свою реализацию
        return null;
    }
}
